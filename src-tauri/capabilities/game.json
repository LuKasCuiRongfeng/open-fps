{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "game",
  "description": "Capability for the game window",
  "windows": ["game"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:window:allow-close",
    "core:window:allow-destroy"
  ]
}